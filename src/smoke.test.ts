import { describe, expect, it } from 'vitest'

// Vérifie seulement que le harnais de test tourne. Remplacé par les tests du
// moteur au lot 1.
describe('harnais de test', () => {
  it('exécute une assertion', () => {
    expect(1 + 1).toBe(2)
  })
})
